name: flutter_compatibility
description: "An API version-dependent widget management library"
version: 0.1.0
homepage: https://github.com/eternity1984/flutter_compatibility

environment:
  sdk: '>=3.2.1 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  version: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

